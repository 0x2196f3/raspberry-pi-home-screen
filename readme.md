# Raspberry-Pi-Home-Screen

#### display home-assistant control pannel on raspberry-pi on startup

- only tested on raspberry-pi4, latest version of raspberry-pi OS
- firefox browser is need
# Usage
- download project
```bash
sudo bash ./install.sh
```
- set your homeassistant server url, username and password in config.py
- reboot


